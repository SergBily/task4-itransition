import React from 'react';
import './home.scss';

const Home: React.FC = (): JSX.Element => (
  <div className="home__container">
    <h1 className="home__title"> WELCOME TO USER MANAGEMENT APP!</h1>
  </div>
);

export default Home;
